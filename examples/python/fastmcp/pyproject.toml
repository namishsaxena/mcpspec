[project]
name = "mcpspec-example-task-manager-fastmcp"
version = "0.0.0"
description = "Task Manager example using FastMCP + mcpspec"
requires-python = ">=3.10"
dependencies = [
    "mcpspec-dev",
    "mcp>=1.26.0",
    "uvicorn>=0.31.1",
]
